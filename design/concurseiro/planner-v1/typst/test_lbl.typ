
#import "estilos.typ": *
#show: planner-page
#grid(columns: (auto, 1fr, auto), column-gutter: 2.2mm,
  align: (left + horizon, left + horizon, right + horizon),
  box(width: 4pt, height: 4pt, fill: accent),
  lbl("Metas da semana — rotação & volume", track: 0.10em),
  [],
)
